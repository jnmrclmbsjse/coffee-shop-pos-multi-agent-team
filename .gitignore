# Dependencies and generated output
node_modules/
dist/
coverage/
*.tsbuildinfo
apps/api/src/generated/prisma/

# Local configuration
.env
.env.*
!.env.example
!deploy/.env.prod.example

# IDE and operating system
.idea/
.DS_Store
*.swp

# Logs
*.log
pnpm-debug.log*

# Claude
.claude

# Poller runtime
logs/
.poll-state/
.poll-stop

# Pulumi local state / plugin cache
infra/bin/
infra/.pulumi/

# Playwright browser profile / cache from the discovery agent
.playwright/
.playwright-mcp/
playwright-report/
test-results/

# Discovery working artifacts (screenshots etc). These are evidence the agent
# looked at, not deliverables — the findings file is the deliverable. If a
# screenshot is genuinely worth keeping, move it out deliberately.
artifacts/
docs/discovery-artifacts/
*.png
!docs/design/**/*.png