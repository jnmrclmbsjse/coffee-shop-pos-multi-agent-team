name: Epic
description: A feature/initiative containing multiple stories
title: "[EPIC] "
# NOTE: this template deliberately applies NO `agent:*` routing label.
# Routing labels are what the poller dispatches on — applying one at
# creation makes an issue eligible for pickup before its gating
# conditions are met (e.g. a QA task picked up before its dev tasks
# merged, or a blocked dev task dispatched early). Agents/scripts apply
# `agent:*` deliberately when the work is actually ready.
labels: ["type:epic"]
body:
  - type: textarea
    id: summary
    attributes:
      label: Summary
    validations:
      required: true
  - type: dropdown
    id: moscow
    attributes:
      label: MoSCoW Priority
      options: ["Must", "Should", "Could", "Won't"]
    validations:
      required: true
  - type: textarea
    id: stories
    attributes:
      label: Stories
      description: Linked story issue numbers (rolls up automatically if using native sub-issues on an org repo; otherwise maintain as a checklist here)